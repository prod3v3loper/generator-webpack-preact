/**
 * Production Plugins
 * We can add here all plugins we want to use for Production envoirement, only in production
 * To do this you add in generator.js to topScopes your plugin
 */

export default function proPlugins(answer) {
  return [""];
};
