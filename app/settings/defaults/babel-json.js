

// babel.config.json
module.exports = function createBabelJson(answer) {
    const babelJsonConf = {
        // Example, we have our config for that in webpack config
        // "targets": {
        //     "chrome": "58",
        //     "ie": "11"
        // }
    };

    return babelJsonConf;
};